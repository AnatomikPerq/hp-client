display_name: HYPER CLIENT
package_name: hyperclient
maintainer:
  name: HYPER CLIENT
  email: noreply@huhusova67.ru
priority: optional
section: net
# Fallback for direct Fastforge use. The App build script replaces this with
# the release bundle's actual size before creating the DEB.
installed_size: 130000
dependencies:
  - procps
  - libcap2-bin
  - libayatana-appindicator3-1
essential: false
icon: assets/logo.png
postinstall_scripts:
  - setcap cap_net_admin,cap_net_raw+eip /opt/HyperClient/bin/HyperClientCore
keywords:
  - HYPER CLIENT
  - Xray
  - VPN
  - Proxy
generic_name: VPN and Proxy Client
categories:
  - Network
startup_notify: true
startup_wm_class: HyperClient
supported_mime_type:
  - x-scheme-handler/hyperclient
